import ReactDOM from 'react-dom'; // import a react object that has the render() function

import './index.css'; // import a CSS file to select the style
import App from './App'; // import a components from ./App file and mane it App 

ReactDOM.render(<App />, document.getElementById('root')); // display the App conponent on the main page(root)
